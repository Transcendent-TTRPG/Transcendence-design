# Reports

Use this directory for experiment outputs.

- `raw/` for machine-readable dumps
- `summaries/` for human-readable synthesis
- `charts/` for optional exported figures
- `technique-balance-matrix.md` for the current general cost map across all
  simulation-defined Techniques
